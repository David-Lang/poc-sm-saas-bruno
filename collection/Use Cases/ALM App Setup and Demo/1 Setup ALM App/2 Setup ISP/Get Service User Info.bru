meta {
  name: Get Service User Id
  type: http
  seq: 1
}

post {
  url: https://{{IspTenantId}}.id.cyberark.cloud/UserMgmt/GetUserInfo
  body: text
  auth: none
}

headers {
  X-CENTRIFY-NATIVE-CLIENT: true
  Authorization: Bearer {{identityToken}}
}

tests {
  test("Set EnVar serviceUserId", function() {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(200);
    bru.setEnvVar("serviceUserId", res.getBody().Result.Id)
  });
}
